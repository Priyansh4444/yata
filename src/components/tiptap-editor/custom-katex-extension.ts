import { Extension, Extensions } from "@tiptap/core";
import katex from "katex";

// TODO: finish making an extension which whenever you detect a node starting with $$ make a <HTML ELEMENT> for block adn then for $ make it inline and add an event listener for every update to re-render those blocks and to kinda like add and remove event listeners as they come and go and then the rich editor should be done.
